# Core ML libraries (updated for compatibility)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# API framework (for later)
flask>=3.0.0

# Data validation (for later)
pydantic>=2.0.0

# Testing (for later)
pytest>=7.4.0

# Utilities
python-dotenv>=1.0.0
